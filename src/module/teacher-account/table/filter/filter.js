import { Flex } from '@chakra-ui/react';
import { memo } from 'react';

const Filter = () => {
  return <Flex mb={10} w="full" justifyContent="space-between"></Flex>;
};

export default memo(Filter);
